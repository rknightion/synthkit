# MUST FAIL: a control Secret that does not actually project CONTROL_TOKEN.
controlPlane:
  exposure:
    ack: tls-proxy
credentials:
  control:
    existingSecret: synthkit-control
    keys:
      GIT_TOKEN: GIT_TOKEN
