# MUST FAIL: self-observability would authenticate against the synthetic-data credential object.
config:
  dryRun: false
selfObs:
  enabled: true
credentials:
  data:
    existingSecret: synthkit-data
  selfObs:
    existingSecret: synthkit-data
    keys:
      GC_SELF_OTLP_PASSWORD: GC_SELF_OTLP_PASSWORD
