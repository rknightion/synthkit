---
id: SKT-0013.04
title: Run the k3d lab as a parallel permutation matrix with one combined report
status: In Progress
assignee: []
created_date: '2026-08-27 08:34'
updated_date: '2026-08-27 08:35'
labels: []
dependencies: []
parent_task_id: SKT-0013
priority: high
type: feature
ordinal: 75000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The lab runs one configuration, serially, and prints what it found. SKT-0013 needs it to run several permutations, and the emission-validation work needs it to run alongside them, so the harness itself is now the constraint.

Restructure it so each permutation is an independent job that can run in parallel, and the run ends in ONE report covering all of them.

The report is the deliverable, not a side effect. A matrix that leaves an operator diffing several per-permutation outputs by hand has moved the work rather than done it. It needs to say, in one place: which permutations ran, which produced a capture, where two permutations DISAGREE about the same metric family, and what that disagreement means — because two permutations differing is a real property of the estate, not a defect to reconcile.

Three properties that are easy to lose and expensive to get back:

- **A failed permutation must be distinguishable from one that captured nothing.** Both produce an empty corpus entry, and they mean opposite things.
- **Partial success must not read as success.** If three of four permutations capture and the fourth times out, the combined report says so loudly rather than presenting three green rows.
- **Parallel jobs must not collide on the corpus.** Each permutation writes its own tagged entry; merging is a deliberate step, not a race between jobs.

Docker is an exclusive resource, so the parallelism has to be bounded and the bound stated rather than discovered by exhausting the host.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Permutations run as independent jobs, in parallel, with a stated concurrency bound
- [ ] #2 One combined report covers every permutation in the run
- [ ] #3 A failed permutation is distinguishable from one that captured nothing
- [ ] #4 Partial success is reported as partial, never as success
- [ ] #5 Where permutations disagree about a metric family, the report names the disagreement rather than reconciling it
- [ ] #6 Parallel jobs cannot corrupt each other corpus output
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 make gate (build vet test race rw-proto-check spdx-check forbidden-words)
- [ ] #2 make blueprint-schema (only if a blueprint field or construct/workload config struct changed)
- [ ] #3 DRY_RUN=true go run ./cmd/synthkit -once -dump — inventory diffed against signals/
<!-- DOD:END -->
